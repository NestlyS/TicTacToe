<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mygame">

    <application
        android:allowBackup="true"
        android:icon="@drawable/cross_new_anim_light_7"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".TicTacToe.multi.NameEnterActivity"
            android:label=""
            android:theme="@style/Theme.MaterialComponents.Dialog"
            android:windowSoftInputMode="stateVisible"></activity>
        <activity
            android:name=".TicTacToe.multi.MultiplayerActivity"
            android:label="@string/title_activity_multiplayer"
            android:theme="@style/NoActionBar" />
        <activity android:name=".TicTacToe.TicTacToeActivity" />
        <activity
            android:name=".TicTacToe.multi.RoomNameEnterActivity"
            android:label=""
            android:theme="@style/Theme.MaterialComponents.Dialog"
            android:windowSoftInputMode="stateVisible" />
        <activity
            android:name=".MenuActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AboutActivity"
            android:label="@string/about"
            android:theme="@style/Theme.AppCompat.Light.Dialog" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <service android:name=".MusicService"
            android:enabled="true"/>
    </application>


</manifest>